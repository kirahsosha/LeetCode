/*
 * @lc app=leetcode.cn id=136 lang=cpp
 *
 * [136] 只出现一次的数字
 */

// @lc code=start
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        if(nums.size() == 1) return nums[0];
        vector<int> n((nums.size() + 1) / 2);
        int count = 0;
        for(int i = 0; i < nums.size(); i++)
        {
            
        }
        return 0;
    }
};
// @lc code=end

